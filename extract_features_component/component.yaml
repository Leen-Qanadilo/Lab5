
$schema: https://azuremlschemas.azureedge.net/latest/commandComponent.schema.json

name: extract_features_component
display_name: Extract Features Component
version: 1
type: command

inputs:
  raw_images:
    type: uri_folder

outputs:
  features_output:
    type: uri_folder

environment: $schema: https://azuremlschemas.azureedge.net/latest/commandComponent.schema.json
name: extract_features_component
display_name: Extract MRI Features
version: 1
type: command

inputs:
  input_data:
    type: uri_folder

outputs:
  features_output:
    type: uri_file

code: ../src

environment: azureml:AzureML-pytorch-2.0-py310@latest

command: >-
  python extract_features.py
  --input_data ${{inputs.input_data}}
  --output_path ${{outputs.features_output}}


code: .

command: >-
  python extract_features.py
  --raw_images ${{inputs.raw_images}}
  --features_output ${{outputs.features_output}}
